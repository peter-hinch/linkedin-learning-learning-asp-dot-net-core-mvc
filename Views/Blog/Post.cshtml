@* Define the model which will be used by this view. *@
@model LearningAspDotNetCoreMVC.Models.Post

@* Insert a partial view using the following syntax. It is an asynchronous method
   so await must be used.*@
@await Html.PartialAsync("_Post")
